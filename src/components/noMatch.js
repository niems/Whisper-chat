import React from 'react';

const NoMatch = () => {
  const offsiteMsg = 'There is nothing here, come back to the light...';

  return (
    <article className="wrapper center">
      <h1>{offsiteMsg}</h1>
    </article>
  );
};

export default NoMatch;
