import React from 'react';

const NoMatch = (props) => {
    return (
        <article className='wrapper center'>
            <h1>Don't be a dick, stay on the site :P</h1>
        </article>
    );
};

export default NoMatch;